export const avatar = () => {
    return({
    height: "1.7rem",
    width: "1.7rem"
    }
    )
}

export const avatarGroup = () => {
    return({
        display: "flex",
    alignSelf: "right",
    height: "1.7rem",
    // alignItems: "center",
    // width: "fit-content",
    marginTop: "2px",
    marginRight: "3px"
    }
    )
}